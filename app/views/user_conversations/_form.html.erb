<%= form_for(@user_conversation) do |f| %>
  <% if @user_conversation.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user_conversation.errors.count, "error") %> prohibited this user_conversation from being saved:</h2>

      <ul>
      <% @user_conversation.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_id %><br>
    <%= f.number_field :user_id %>
  </div>
  <div class="field">
    <%= f.label :conversation_id %><br>
    <%= f.number_field :conversation_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
