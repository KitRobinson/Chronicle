<%= form_for(@myths_user) do |f| %>
  <% if @myths_user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@myths_user.errors.count, "error") %> prohibited this myths_user from being saved:</h2>

      <ul>
      <% @myths_user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_id %><br>
    <%= f.text_field :user_id %>
  </div>
  <div class="field">
    <%= f.label :myth_id %><br>
    <%= f.text_field :myth_id %>
  </div>
  <div class="field">
    <%= f.label :access_level %><br>
    <%= f.text_field :access_level %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
