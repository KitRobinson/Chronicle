<%= form_for @myth, html: { multipart: true } do |f| %>
  <% if @myth.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@myth.errors.count, "error") %> prohibited this myth from being saved:</h2>

      <ul>
      <% @myth.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :myth_pic %><br>
    <%= f.file_field :myth_pic %>
  </div>
  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :content %><br>
    <%= f.text_area :content %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
